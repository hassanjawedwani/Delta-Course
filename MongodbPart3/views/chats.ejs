<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chats</title>
  <link rel="stylesheet" href="styles.css"/>
</head>
<body>
  <form action="http://localhost:8080/chats/new" method="get">
    <button class="chat" type="submit">Add new Chat</button>
  </form>
  <% for (let i = 0; i < chats.length; i++) { %>
    <div class="chat">
      <p><strong>Id : </strong><%=chats[i].id%></h>
      <p><strong>From:  </strong><%=chats[i].from%></p>
      <p><strong>To:  </strong><%=chats[i].to%></p>
      <p class="msg"><strong>Message:</strong><%=chats[i].message%></p>
      <form action="http://localhost:8080/chats/<%=chats[i]._id%>/edit" method="get"><button type="submit">Edit message</button></form>
      <p><strong>Time:  </strong><%=chats[i].createdAt.toString().split(" ")[4]%></p>
      <p><strong>Date:  </strong><%=chats[i].createdAt.toString().split(" ").splice(0, 4).join(" ")%></p>
      <br/>
      <form action="http://localhost:8080/chats/<%=chats[i]._id%>?_method=DELETE" method="post"><button type="submit">Delete message</button></form>
    </div>
  <% } %>
  <form action="http://localhost/chats/new" method="get">
    <button type="submit">Add new Chat</button>
  </form>
</body>
</html>

